[project]
name = "noah-code"
version = "0.2.4"
description = "Noah Code terminal coding agent, built on NVIDIA OO Agents (NOOA)"
readme = "README.md"
requires-python = ">=3.12,<3.14"
license = { text = "Apache-2.0" }
authors = [{ name = "noah-code contributors" }]
keywords = ["agents", "coding", "nooa", "terminal"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: MacOS",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development",
]

dependencies = [
    "nooa==0.0.9",
    "nooa-cli==0.0.9",
    # LiteLLM 1.92.0-1.95.x intermittently shipped without macOS wheels,
    # forcing a Rust source build. Wheels are back for every release since
    # 1.96.0 (abi3 since 1.96.2), so the installer stays binary-only.
    "litellm>=1.96.0,<1.99.0",
    # Upper bounds guard fresh end-user installs (uv tool install / pip),
    # which ignore uv.lock; caps always admit the currently locked versions.
    "click>=8.1.0,<9.0.0",
    "pydantic>=2.5.0,<3.0.0",
    "PyYAML>=6.0.0,<7.0.0",
    "packaging>=24.0,<27.0",
    "rich>=13.0.0,<16.0.0",
    "textual>=1.0.0,<9.0.0",
]

[project.urls]
Homepage = "https://github.com/skundu42/noah-code"
Documentation = "https://github.com/skundu42/noah-code#readme"
Repository = "https://github.com/skundu42/noah-code.git"
Issues = "https://github.com/skundu42/noah-code/issues"
Releases = "https://github.com/skundu42/noah-code/releases"

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=5.0.0",
    "pytest-socket>=0.7.0",
    "ruff>=0.8.0",
    "mypy>=2.3.1",
    "types-pyyaml>=6.0.12.20260815",
]
mcp = [
    "nooa[mcp]==0.0.9",
]
tracing = [
    "nooa[tracing]==0.0.9",
    "opentelemetry-exporter-otlp-proto-http>=1.44.0,<2.0.0",
]

[project.scripts]
noah-code = "noah_code.cli:main"
nc = "noah_code.cli:main"
noah = "noah_code.cli:main"

[build-system]
requires = ["hatchling>=1.26.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/noah_code"]

[tool.hatch.build.targets.wheel.force-include]
"src/noah_code/ui/textual.css" = "noah_code/ui/textual.css"
[tool.uv]
package = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
# Keep the default suite hermetic; opt in with `pytest -m integration`.
# Sockets are blocked by default (pytest-socket); AF_UNIX stays allowed
# because asyncio event loops create a local self-pipe socketpair.
addopts = ["-m", "not integration", "--disable-socket", "--allow-unix-socket"]
markers = [
    "integration: tests that may touch the network (deselect by default)",
]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.12"
files = ["src/noah_code"]
check_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
# NOOA's tool/skill framework stacks @hidden/@property and spec() decorators
# in ways mypy's plugin model does not describe; these codes are noise here.
disable_error_code = ["prop-decorator", "empty-body"]

[[tool.mypy.overrides]]
# nooa resolves attributes lazily (PEP 562) and litellm ships no stubs;
# treat every public surface as Any instead of half-analyzing sources.
module = ["nooa", "nooa.*", "nooa_cli", "nooa_cli.*", "litellm", "litellm.*"]
follow_imports = "skip"
ignore_missing_imports = true
